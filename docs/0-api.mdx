export const API = ({name, type, desc, more}) => (
    <div style={{marginBottom: "8px"}}>
        <div><span style={{color: "rgb(33, 150, 243)", fontWeight: "700"}}>{name}</span> → <span style={{color: "rgb(33, 150, 243)", fontWeight: "400"}}>{type}</span></div>
        <div style={{fontSize: "14px"}}>{desc}</div>
        <div><span style={{color: "#808080", fontSize: "13px"}}>{more}</span></div>
    </div>
)